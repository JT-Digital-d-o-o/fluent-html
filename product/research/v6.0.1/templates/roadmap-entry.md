---
rfc: RFC-<TRACK>-<NN>
milestone: <6.0.1|6.1.0|parked-major|wontfix>
priority: <P0|P1|P2>
effort: <S|M|L|XL>
breaking: <false|additive|breaking>
ships_with: []              # RFC ids to bundle into one release
score: <impact×pain×reach / effort — from the rubric>
---

# Roadmap entry: RFC-<id>

## Summary
One paragraph: what ships, for whom.

## Placement rationale
Why this milestone / priority. 6.0.1 = strictly bug/behavior fixes (no shape change). 6.1.0 = additive. parked-major = needs a break. wontfix = considered and declined (say why).

## Dependencies
Must ship after: [RFC ids]. Reason.

## Migration impact
6.0.1 fix (more-correct output — note any behavior delta) · additive (no action) · parked (needs a major).
