/**
 * `@fluent-html` class vocabulary (C-05) — the single source of truth for the
 * method↔Tailwind-class mapping. Consumed by the library (`prefixOf`), the
 * extractor, and the ESLint plugin (the latter two generated from `classVocab`).
 *
 * @module
 */
export { UNITS, DIR_MAP, ROUNDED_CORNERS, defineUtility } from "./types.js";
export { classVocab } from "./vocab.js";
export { emitClasses, prefixOf } from "./emit.js";
export { DIRECT_VARIANTS, variantKeySpecs } from "./variant-keys.js";
//# sourceMappingURL=index.js.map