---
rfc: RFC-A-004
implemented: yes
---

# Juxtaposition: RFC-A-004 verdicts vs shipped code

RFC-A-004 (extractor ↔ eslint class-vocab integrity: kill spurious classes, derive UNITS, add reverse parity guard). All three Wave-3 verdicts returned `survives-with-changes` with no killer objection. Implementation state: **shipped**. Each `required_change` below is reconciled against the actual code in `fluent-html-tailwind-extractor`, `fluent-html-eslint-plugin`, and `fluent-html/test`.

| change | lens | status | severity | evidence | action |
|---|---|---|---|---|---|
| Correct the "strict subset" claim: F-A-161's new regex also EMITS new tokens (`bg-[url(/x.png)]` intact) — restate as junk-dropped + arbitrary-`[...]`-kept, both safelist-safe, not literally a subset. | breaking-change | partial | low | The fix shipped correctly (`extract.ts:233-238`, `extractDefaultClasses` keeps `bg-[url(/x.png)]` intact — test `extract.test.ts:197-199`). But the CHANGELOG `[6.0.1]` §Tooling (`CHANGELOG.md:225-230`) and the RFC §Compatibility (`RFC-A-004.md:223-230`) still describe the change without the "not literally a subset; new arbitrary tokens are now produced" nuance. The §Compatibility "strict subset" line is uncorrected prose. | Amend RFC §Compatibility line "returns a *strict subset*" to state F-A-161 also produces new (valid, intact-arbitrary) tokens; it is safelist-safe in both directions but not a literal subset. Prose only; code is correct. |
| State explicitly that extractor (2.0.0) and eslint-plugin (1.7.0) are SEPARATELY versioned from the lib (6.0.x); bump/changelog those packages on their own tracks, don't fold their behavior change silently under the lib's 6.0.1. | breaking-change | partial | medium | Versions confirmed separate: `fluent-html-tailwind-extractor/package.json:3` = `2.0.0`, `fluent-html-eslint-plugin/package.json:6` = `1.7.0`. The eslint plugin HAS a CHANGELOG with a `[1.7.0]` entry. **The extractor has NO CHANGELOG file at all** (`find` shows none), so its behavior change (F-A-160/F-A-161, exported `extractDefaultClasses`/`scan`/`extractClasses`) is recorded only in the LIB's `CHANGELOG.md:225-230` under 6.0.1, exactly the conflation the verdict warned against. | Add a `CHANGELOG.md` to `fluent-html-tailwind-extractor` documenting the F-A-160/F-A-161 behavior changes under its own 2.0.0 (or a patch entry); add the F-A-162 `VOCAB_UNITS` change to the eslint plugin's `[1.7.0]` changelog body if not already listed. |
| Add a snapshot/regression-churn note: downstream consumers asserting OLD spurious output (bare `bg-blue-600`, partial-prefix `hover:bg-red-500`, split `bg-[url(/x.png)`) will see test churn — document as expected. | breaking-change | missing | low | No churn/migration note found in extractor docs (no extractor CHANGELOG) or the lib CHANGELOG `[6.0.1]` §Tooling. The behavior change is real (`extract.test.ts:40-45` asserts the old spurious classes are now absent) but no consumer-facing note about expected snapshot churn exists. | Add a one-line "downstream safelist snapshots will shrink (fewer classes) — expected, CSS-safe" note to the extractor changelog/release notes. Low impact (smaller safelist is always safe). |
| F-A-161: make residual non-paren dotted tokens (`.setHtmx`, `routes.list`) explicit as harmless non-matching tokens — don't imply full call-expr erasure; the fix kills only paren-bearing tokens. | correctness | partial | low | Code is correct: `extractDefaultClasses` (`extract.ts:233-238`) only guarantees no `(`/`)` in tokens — test `extract.test.ts:194` asserts exactly that ("no token contains a paren"), not that `routes.list` is dropped. The JSDoc at `extract.ts:234-236` says it won't swallow a call "whole" (accurate). But neither RFC prose ("no longer swallows call expressions", `RFC-A-004.md:252`) nor CHANGELOG (`:228`) states the dotted residue remains. | Reword RFC/CHANGELOG to "no longer swallows a fluent call expression *whole* as a single token; paren-bearing tokens are dropped, harmless dotted residue (`routes.list`) remains and is ignored by Tailwind." Code needs no change. |
| F-A-163: pin the scan to PLAIN string-literal `addClass`/`setClass` bodies only (exclude `${}` template literals); state bare-literal `=== undefined` branches must be matched-and-passed via vocab, not skipped. | correctness / type-safety | applied | medium | `class-vocab.test.ts:220-224`: the `emits` predicate is `/this\.addClass\(\s*[`"'][:\w[-]/` — it fires on a backtick too, but the test reports a method as missing only when it `emits && !vocab.has(name) && !STRUCTURAL.has(name)` (`:237-239`). Every interpolated-template emitter (`bg-${color}`) is a registered vocab method, so it is matched-and-passed, never reported. Bare-literal emitters (`flex`, `@container`) are likewise in `classVocab`. The "teeth" test (`:230-233`) asserts ≥100 emitters are found, so the predicate is not silently skipping them. Net behavior matches the requirement: registered emitters pass, an unregistered plain-literal emitter would fail. | None. The intent (registered emitters pass, unregistered literal emitter fails) is satisfied; the predicate including backticks is harmless because all template emitters are registered. |
| F-A-163: resolve the Open Question — adopt `src/core/*-methods.ts` glob OR lock the two-file list, after confirming only tailwind-methods.ts + htmx-methods.ts emit literal classes in core. | correctness | applied | low | `class-vocab.test.ts:213`: `SOURCES = ["tailwind-methods.ts", "htmx-methods.ts"]` — the two-file list is locked (not a glob). The correctness verdict explicitly verified these are the only core files emitting literal classes, so the hardcoded list is a sound resolution of the open question. | None. A future new `*-methods.ts` emitter file would silently escape the guard; consider migrating to a `src/core/*-methods.ts` glob for future-proofing (optional, low value). |
| F-A-163: anchor the static guard on the `p.<m> = function` assignment / scan only that body — NOT a free `addClass("...")` grep — so JSDoc `@example` `addClass(...)` in tag.ts:113/242 don't false-positive the test red. Add a fixture for commented addClass. | type-safety | applied | medium | `class-vocab.test.ts:220` anchors on `defRe = /(?:^|\n)\s*p\.([A-Za-z_$][\w$]*)\s*=\s*function\b.../` — only method-assignment bodies are scanned. Moreover `SOURCES` (`:213`) excludes `tag.ts` entirely, so the two JSDoc `addClass("bg-white rounded")` / `addClass("border-red-500 text-red-700")` examples (`tag.ts:113,242`) are doubly out of scope. The guard is green on the shipped tree. | None on code. The explicit "commented addClass does not trip the guard" fixture the verdict requested was not added, but it is moot — tag.ts isn't even in SOURCES. Optional: add a comment in the test noting the method-assignment anchor + SOURCES list jointly exclude comments. |
| F-A-163: exclude interpolated template-literal emitters via a decidable "class-shaped literal = StringLiteral (no interpolation)" predicate; pin with anti-fixture (`addClass(`bg-${x}`)` registered → NOT reported; bare `addClass("tabular-nums")` missing → reported). | type-safety | partial | low | Decidability is achieved in practice (see F-A-163 row above): registered template emitters never report because of the `vocab.has` gate, and `tabularNums` IS registered (`vocab.generated.ts:6`). However the predicate itself (`extract`-side regex `[`"']`) does NOT exclude template literals syntactically — it relies on the vocab gate, not on a StringLiteral-only predicate. The explicit anti-fixture pair the verdict asked for is absent from `class-vocab.test.ts`. | Optional: add the two anti-fixtures (a registered template emitter must not report; an injected unregistered bare-literal emitter must report) to harden against a future regression. Low severity — current tree is correct. |
| F-A-162: amend the "no string-where-a-union-fits is introduced" guardrail line to "preserves the lib's existing UNITS typing; tightening UNITS to a literal union is a separate parked lib change" so the claim isn't overstated. | type-safety | applied | low | `vocab.generated.ts:12`: `VOCAB_UNITS: readonly string[]` mirrors the lib's pre-existing loose `UNITS`. The RFC §Type-safety already states it is "a `readonly string[]` projection of the same `ReadonlySet<string>`" (`RFC-A-004.md:218-219`) — consistent with the verdict's preferred framing; no overstatement shipped. | None. RFC framing already matches the verdict's request. |
| F-A-162: keep `escapeRe` on every unit token in the generated regex even though no current unit needs it; add a test that each VOCAB_UNITS token round-trips through the generated regex. | type-safety | applied | medium | `prefer-unit-overload.ts:6,15-16`: `escapeRe` IS applied to every unit (`.map(escapeRe)`), and tokens are sorted longest-first so a suffix unit can't shadow. Round-trip coverage exists in `eslint-plugin/test/rule.test.js`: every unit has a passing fix case — `px` (`:871`), `vh` (`:876`), `rem` (`:886`), `dvh` (`:894-896`), `svh` (`:948-950`), `lvh` (`:953-955`), `vw` (`:923`), `%` (`:938`), `em` (`:960`). | None. Both `escapeRe` and per-unit round-trip coverage are present. |
| Add the missing ABSENCE assertions to extract.test for F-A-160 (assert `bg-red-500` / `hover:bg-red-500` NOT emitted for the nested example) and a positive/negative pair for F-A-161. | correctness | applied | medium | `extract.test.ts:40-45` adds the negative assertions for F-A-160: `lacks(out, "hover:bg-red-500")`, `lacks(out, "dark:bg-red-500")`, `lacks(out, "bg-red-500")` for `.on("dark", t => t.on("hover", t => t.background("red-500")))`, plus the positive `has(out, "dark:hover:bg-red-500")`. F-A-161 has a positive (`:197-199` keeps `bg-[url(/x.png)]`) and a negative (`:191-195` drops `setHtmx(routes.list)` + asserts no paren in any token). The `lacks` helper (`:7`) is the absence primitive the verdict required. | None. The exact absence/negative assertions the verdict flagged as missing are present. |
| Killer objection | (all 3 lenses) | moot | — | All three verdicts: `killer_objection: null`. No kill to reconcile. | — |

## Notes on shipped correctness (beyond the verdicts)

- **F-A-160** is implemented exactly as designed: `blankVariantBodies` (`extract.ts:173-189`) blanks top-level `.on/.at` bodies preserving offsets; `extractVariantClasses` scans `blankVariantBodies(body)` flat then recurses on the original `body` (`:214-219`); `scanInternal` runs the flat vocab scan over `blankVariantBodies(stripped)` (`:259-262`). The triple-emit is closed.
- **F-A-162** drift guard is live: `vocab-drift.mjs:35-39` asserts `VOCAB_UNITS` matches the lib's `UNITS`, the same mechanism pinning `UNIT_METHODS`. `gen-vocab.mjs:14,27,40` reads and emits `UNITS`.
- The reverse-parity guard (`class-vocab.test.ts:235-241`) passes on the shipped tree: `htmxIndicator` is registered (`vocab.generated.ts:6`) and the structural allowlist (`:212`) includes the extra `setClasses`/`whenElse` touchers present in the real code.
