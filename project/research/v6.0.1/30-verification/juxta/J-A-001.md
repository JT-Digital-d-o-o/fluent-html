---
rfc: RFC-A-001
implemented: reconciled
---

# Juxtaposition: RFC-A-001 vs Wave-3 verdicts vs shipped code

RFC-A-001 (boundary-aware route-param substitution) is **implemented** in `src/routes.ts`.
The shared helper `substituteParams` (`src/routes.ts:179-186`) + `escapeRegExp` (`src/routes.ts:170-172`)
ship exactly as proposed, both call sites use it (`src/routes.ts:282`, `src/routes.ts:292`), the
A-001 test suite is green (`test/routes.ts:476-493`, 109/109 routes tests pass), and the CHANGELOG
entry landed (`CHANGELOG.md:221`). The verdicts' `required_change`s are almost entirely about
**RFC prose accuracy** and **test coverage**, not about the shipped mechanism — which all three
lenses agree is correct.

| change | lens | status | severity | evidence | action |
|---|---|---|---|---|---|
| (none) — breaking-change verdict `survives`, `required_changes: []`, `killer_objection: null` | breaking-change | moot | low | `V-RFC-A-001-breaking-change.md:7` empty list. Confirmed in code: `substituteParams`/`escapeRegExp` are non-exported internals (`src/routes.ts:170,179`); no public symbol/type/signature moved; `api_surface: []` accurate. | None. Nothing to reconcile. |
| C1: Fix headline worked example (RFC lines 96-117): `/orgs/:id/users/:idCard` with `{id:42,idCard:"AB-9"}` does NOT corrupt under shipped v6.0.0 (`replace(":id",…)` hits the genuine earlier `:id`), it resolves correctly; the real defect is longer-before-shorter (`/users/:idCard/x/:id`) which THROWS on leftover `:id`. Replace the example. | correctness | missing | low | RFC `RFC-A-001.md:102-117` still asserts the false `/orgs/42/users/42Card` corruption. This is durable-record prose; the shipped CHANGELOG (`CHANGELOG.md:221`) repeats the `:id`/`:idCard` framing but only as "no longer corrupts," which is defensible. The falsified example survives only inside the RFC. | Edit `RFC-A-001.md` §"Worked examples": swap to a longer-before-shorter path (e.g. `/users/:idCard/x/:id` `{id:9,idCard:"C"}` → shipped throws on leftover `:id`) and mark the `/orgs/:id/...` case as the *non-regressing already-correct* path. |
| C2: Correct impact/Problem narrative (RFC lines 28,33): prefix collision does NOT "silently corrupt … with no exception" — leftover placeholder trips `assertNoUnresolvedParams` and THROWS; `encodeURIComponent` shrinks the silent window further. Re-derive `impact: high` from the true loud-throw/narrow-silent failure mode. | correctness | missing | low | RFC `RFC-A-001.md:28,33` retains "silently corrupted … no type error and no exception." Shipped `assertNoUnresolvedParams` (`src/routes.ts:162-167`) does throw on a leftover `:param`, confirming the verdict. Prose-only; the fix itself is correct. | Edit `RFC-A-001.md` Problem §: state the dominant failure is a render-time throw (loud), with a narrow `encodeURIComponent`-bounded silent window; re-justify the priority on that basis. |
| C3a: Add regression test — prefix collision **longer-before-shorter** `/users/:idCard/x/:id` (the genuine v6.0.0 defect case). | correctness | missing | medium | `test/routes.ts:476-493` only covers shorter-before-longer (`/orgs/:id/users/:idCard`, line 478), repeated (`/a/:id/b/:id`, line 479), and the route-callable path. The longer-before-shorter ordering — the case the correctness lens identifies as the actual bug — is **not** asserted. Helper is order-independent so it would pass, but the named defect is uncovered. | Add to the "Boundary-aware param substitution (A-001)" describe: a route `{ method:"get", path:"/users/:idCard/x/:id" }` and assert `.resolve({id:"9",idCard:"C"})` === `/users/9/x/9`-style correct output (no throw, no `9Card`). |
| C3b: Add regression test — prefix collision shorter-before-longer `/orgs/:id/users/:idCard`. | correctness | applied | medium | `test/routes.ts:478,483`: `r.card.resolve({id:"42",idCard:"AB-9"})` asserts `/orgs/42/users/AB-9`. Green. | None. |
| C3c: Add regression test — repeated param `/a/:id/b/:id` → `/a/7/b/7`. | correctness | applied | medium | `test/routes.ts:479,487`: `r.mirror.resolve({id:"7"})` asserts `/a/7/b/7`. Green. | None. |
| C3d: Add regression test — byte-identical single-param `/users/:id` (no regression). | correctness | partial | low | No dedicated assertion inside the A-001 describe, but `prefixedRoutes.detail.resolve({id:"42"})` === `/users/42` (`test/routes.ts:249`) and `typedRoutes` cases (`test/routes.ts:497-513`) exercise single-param substitution. The non-regression intent is covered incidentally, not as an explicit A-001 anchor. | Optional: add one explicit single-param assertion in the A-001 describe to anchor the "previously-correct unchanged" guarantee locally; otherwise the existing prefixed/typed tests suffice. |
| C3-shared: both route callable AND `.resolve()` must be exercised (shared helper). | correctness | applied | medium | `.resolve()` paths: `test/routes.ts:483,487`. Route-callable path: `test/routes.ts:491` (`r.card({id,idCard}).endpoint`). Both hit `substituteParams` (`src/routes.ts:282,292`). | None. |
| T1: Correct overstated grammar-alignment claims (RFC lines 91,138): lookahead does NOT mirror `ExtractParams` (which captures greedily to `/`, yielding names with `.`/`-`/`:`); it mirrors only `assertNoUnresolvedParams`'s detector regex. Rephrase to claim alignment with the DETECTOR only. | type-safety | missing | low | RFC `RFC-A-001.md:91,138` still claims the lookahead "exactly mirrors the identifier grammar used by `ExtractParams`" and "definitionally aligned." Verified false: `ExtractParams` (`src/routes.ts:27-32`) splits on `:` and infers greedily up to `/`. NOTE: the **shipped JSDoc is already correct** — `src/routes.ts:177` says it "mirrors the identifier grammar used by `assertNoUnresolvedParams`," not `ExtractParams`. So the defect is RFC-prose-only; the code is clean. | Edit `RFC-A-001.md:91,138`: change "mirrors `ExtractParams`"/"definitionally aligned" to "mirrors `assertNoUnresolvedParams`'s detector regex `:([a-zA-Z_]\\w*)`." Code needs no change. |
| T2: State explicitly WHY the fix is type-safe despite the grammar divergence: full type-extracted key is built into the pattern via `escapeRegExp`, so the lookahead only governs the trailing boundary; `ExtractParams`'s greedy-to-`/` capture guarantees no typed name is a prefix of adjacent literal path text; lookahead is belt-and-suspenders, real value is disambiguating sibling params (`:id` vs `:idCard`). | type-safety | missing | low | RFC has the "definitionally aligned" hand-wave (`RFC-A-001.md:138`) instead of this honest justification. Shipped code matches the honest story: `escapeRegExp(key)` embeds the whole key (`src/routes.ts:182`). Prose gap only. | Add the explicit justification to `RFC-A-001.md` Type-safety §, replacing the "definitionally aligned" sentence. |
