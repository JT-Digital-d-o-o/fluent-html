# Behavior System v4 — Implementation Plan

## Ordered work items

1. W1 — Emission core (fluent-html): src/behaviors/{events,map,serialize,emit,register}.ts; EVENT_TABLE single source; namespaced attribute grammar; Tag.behavior rewrite; render-time throws; delete hx-on renderers, .hxOn, HxOnEvent, HX_ON_EVENT_RE, escapeJs behavior-path callers. Unit tests pin the wire grammar byte-exactly.
2. W2 — Runtime (fluent-html): src/behaviors/client/ TS dispatcher per spec (capture delegation from EVENT_TABLE, consumption walk, emit-time-remapped events, HTMX_EVENTS const, skew degrade, transient helper with token+compare guard); 10 verb handlers incl. drawer DOM-predicate model with full close routine, nav gating, reconciliation sweep, Escape precedence; publish build -> dist/fluent-behaviors.<version>.js; size-budget CI gate.
3. W3 — Registry + extension toolchain (SIMPLIFIED per ADR-07 amendment — framework-layer-only): registerBehavior (data-only spec, mandatory fixtures, jt: namespace + collision + seal enforcement), defineBehavior client entry, FxCtx, buildBehaviorRuntime esbuild wrapper — all framework-internal APIs, zero app-facing surface. Extended runtime prebuilt ONCE at the framework/template level (no per-app builds, no per-app registry hashes); registry-hash embedding + <html> stamp assertion + server-boot handshake unchanged. Framework extension pack ships jt:listboxNav + jt:timezone.
4. W4 — Acceptance suite (C7): in-package Playwright harness (minimal Fastify app, real strict CSP, pinned htmx); matrix() enumeration from the registry; implement every row in the acceptance matrix incl. two-version skew, production-asset execution, multi-engine state rows, no-htmx profile, native-tier rows.
5. W5 — Framework glue (DECIDED 2026-07-20: lives in projects-template for now — templates/*/src/core/behaviors/ — extraction to @fluent-html/fastify deferred to the framework split; API boundary already drawn so extraction is mechanical): asset route + BehaviorRuntimeScript() atomic registration, immutable headers, X-Fluent-Behaviors refresh hardening on by default, dev render-throw guard. The framework extension pack lives beside it. DialogTag gains setClosedby (fluent-html).
6. W6 — Lint: fluent-html-eslint-plugin rules — raw data-behavior* ban across setDataAttrs/addAttribute/setAttrs, behavior-client-purity for defineBehavior handler bodies, registerBehavior/defineBehavior imports flagged outside framework packages (enforces the framework-only extension policy).
7. W7 — Docs: README behavior section (10/10 verbs), htmx.md:341 rewrite, file-header fix, JSDoc, Wave-8.2 template docs, overlap rule + drawer/modal boundary + MemberPicker negative example + same-verb-once limitation, extension recipe (rs:listboxNav) and timezone-prefill recipe.
8. W8 — Template migration (lockstep PR): delete core/htmx/behaviors.ts + behaviorDispatcherScript; wire plugin + script tag; fix analytics setDataAttrs('back') call site; rewrite tests/unit/behaviors.test.ts to the new grammar; templates/web adds the script tag and upgrades header.ts:92 to drawer.
9. W9 — App migrations (opportunistic, non-blocking): rideshare (~300-line main.js deletion; drawer/clipboard/onClickOutside/resetOnSuccess; jt:listboxNav + jt:timezone consumed from the framework pack — no app-local registration), ttl (delete public/js/main.js; drawer; onEscape at time-entry:679), packages/ui Alert.ts remove{closest}.
10. W10 — Release: single fluent-html minor within the v6 line; template PR merged same day; roadmap/pm updates (#52 resetOnSuccess shipped, #53 display option shipped, v6.0.1 parked dialog deprecation executed).

## Open questions (maintainer input needed)

- ~~Framework-layer packaging~~ RESOLVED 2026-07-20: glue + extension pack live in projects-template (templates/*/src/core/behaviors/) for now; extraction to @fluent-html/fastify happens at the framework split.
- CI cost policy for multi-engine rows: run WebKit + Firefox state-bearing rows on every PR or nightly-only with Chromium per-PR? (Correctness contract is unaffected; this is purely CI budget.)
